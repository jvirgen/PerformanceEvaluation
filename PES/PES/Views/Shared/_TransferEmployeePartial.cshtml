@model PES.ViewModels.TransferEmployeeViewModel

<tbody>
    @foreach (var item in Model.ManagerEmployeeList)
    {
        <tr>
            @Html.Hidden("idEmployee", item.EmployeeId)
            <th>
                @Html.CheckBox("CheckToTransfer", false)
            </th>
            <th>
                @(item.FirstName + " " + item.LastName)
            </th>
        </tr>
    }
</tbody>



